if (CONFIG_ZMK_INPUT_PROCESSOR_INERTIA)
  zephyr_library()
  if(EXISTS "${APPLICATION_SOURCE_DIR}/include")
    zephyr_library_include_directories("${APPLICATION_SOURCE_DIR}/include")
  endif()
  zephyr_library_sources(src/input_processor_inertia.c)
endif()
